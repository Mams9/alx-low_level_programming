#include "main.h"
#include <stdio.h>

/**
 * main - this prints out the name of the arguments
 * @argc: the number of command lines arguments
 * @argv: Array name of the argument
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
